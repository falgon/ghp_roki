dy(function () {
	      wsize = $(window).width();
	            $(".rokiicon").css("height", (wsize / 2.6) + "px");
});
    
$(window).resize(function () {
	     wsize = $(window).width();
	          $(".rokiicon").css("height", (wsize / 2.6 ) + "px");
});
